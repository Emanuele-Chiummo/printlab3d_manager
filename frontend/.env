# Imposta l'URL base per le chiamate API dal frontend
# In sviluppo con vite: usa http://localhost:9000 se backend gira in locale
# In produzione con Docker: usa http://localhost:9000
VITE_API_URL=http://localhost:9000
